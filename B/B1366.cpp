#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
void solve(){
    int n,x,m,l,r,lid;
    cin>>n>>x>>m;
    vector<pair<int,int> > v(m,{0,0});
    for(int i=0;i<m;i++) cin>>v[i].first>>v[i].second;
}
int main(){
    int t;
    cin>>t;
    while(t--){
        solve();
    }
}